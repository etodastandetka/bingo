# Скрипт создания пользователя для админ-панели

## Описание

Скрипт `create-user.ts` позволяет создавать новых пользователей для входа в админ-панель с валидацией данных и интерактивным вводом.

## Использование

### Интерактивный режим (рекомендуется)

Запустите скрипт без аргументов, и он запросит все необходимые данные:

```bash
npm run create-user
```

или

```bash
cd admin
npx tsx scripts/create-user.ts
```

### Режим с аргументами командной строки

Можно передать данные через аргументы:

```bash
npm run create-user <username> <password> [email] [isSuperAdmin]
```

**Примеры:**

```bash
# Создать обычного пользователя
npm run create-user myuser mypassword123

# Создать пользователя с email
npm run create-user myuser mypassword123 user@example.com

# Создать суперадмина
npm run create-user admin admin123 admin@example.com true
```

## Параметры

- `username` (обязательно) - Имя пользователя (3-50 символов, только буквы, цифры и подчеркивание)
- `password` (обязательно) - Пароль (минимум 6 символов)
- `email` (опционально) - Email адрес (должен быть уникальным)
- `isSuperAdmin` (опционально) - `true` или `1` для создания суперадмина, иначе обычный пользователь

## Валидация

Скрипт проверяет:
- ✅ Уникальность имени пользователя
- ✅ Уникальность email (если указан)
- ✅ Минимальную длину пароля (6 символов)
- ✅ Формат email (если указан)
- ✅ Формат имени пользователя (только буквы, цифры, подчеркивание)

## Пример использования

```bash
$ npm run create-user

🔐 Создание нового пользователя для админ-панели

Введите имя пользователя: operator1
Введите пароль: ********
Подтвердите пароль: ********
Введите email (необязательно, нажмите Enter чтобы пропустить): operator@bingo.com
Создать суперадмина? (y/n, по умолчанию n): n

✅ Пользователь успешно создан!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👤 Имя пользователя: operator1
📧 Email: operator@bingo.com
🔑 Пароль: ********
⭐ Суперадмин: Нет
🆔 ID: 2
📅 Создан: 04.01.2026, 12:00:00
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  ВАЖНО: Сохраните пароль в безопасном месте!
   Рекомендуется изменить пароль после первого входа.
```

## Примечания

- Пароль хешируется с помощью bcrypt перед сохранением в базу данных
- Все пользователи создаются с флагом `isActive: true`
- Суперадмины имеют полный доступ ко всем функциям админ-панели
- Скрипт автоматически проверяет уникальность имени пользователя и email

