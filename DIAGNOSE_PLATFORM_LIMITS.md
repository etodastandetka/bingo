# 🔍 Диагностика проблем с лимитами платформ

## 📊 Текущий статус:

✅ **Работают:**
- 1xbet: 923176.57 с
- 1WIN: 123548.29 с
- Winwin: 290550.30 с
- 888starz: 181761.43 с
- 1xCasino: 56495.72 с
- BetWinner: 39607.60 с
- WowBet: 8919.97 с

❌ **Не работают (показывают 0.00):**
- Melbet: 0.00 с (ошибка 403 Forbidden в логах)
- Mostbet: 0.00 с

## 🔧 Диагностика:

### 1. Проверка конфигурации в БД

```bash
cd /var/www/bingo_bot/admin
npx tsx scripts/check-platform-limits.ts
```

Этот скрипт покажет:
- Есть ли конфигурация в БД для каждой платформы
- Какие параметры настроены
- Работает ли функция `getCasinoConfig()`

### 2. Проверка логов на ошибки

```bash
pm2 logs bingo-admin | grep -i "melbet\|mostbet\|403\|401"
```

Ищите:
- `[melbet] API error (403)` - ошибка авторизации
- `[melbet] API error (401)` - неверный пароль
- `[Platform Limits] Melbet: конфигурация не найдена` - проблема с конфигурацией

### 3. Обновление паролей

Если пароли не обновлены в БД:

```bash
cd /var/www/bingo_bot/admin
npx tsx scripts/update-casino-passwords.ts
```

Проверьте, что пароль для Melbet обновлен на `pUdKHv4SoV`

### 4. Проверка конфигурации Mostbet

Mostbet использует другой API (не Cashdesk), нужны:
- `api_key`
- `secret`
- `cashpoint_id`

Проверьте, что в БД есть запись с ключом `mostbet_api_config`:

```bash
cd /var/www/bingo_bot/admin
npx prisma studio
# Или через SQL проверьте таблицу BotConfiguration
```

### 5. Ручная проверка API

Для Melbet (если нужно проверить вручную):
- URL: `https://partners.servcul.com/CashdeskBotAPI/Cashdesk/{cashdeskid}/Balance`
- Требуется правильная подпись с паролем `pUdKHv4SoV`

## 🐛 Возможные причины проблем:

### Melbet (403 Forbidden):
1. **Неверный пароль** - пароль не обновлен в БД
2. **Блокировка API** - API временно блокирует запросы
3. **Неправильная подпись** - ошибка в генерации подписи запроса
4. **Истек срок действия пароля** - нужно обновить пароль

### Mostbet (0.00):
1. **Конфигурация не найдена** - нет записи `mostbet_api_config` в БД
2. **Неверные credentials** - неправильный `api_key` или `secret`
3. **Ошибка API** - проблема с подключением к API Mostbet
4. **Неправильный cashpoint_id** - неверный ID кассы

## ✅ Решение:

1. **Запустите диагностический скрипт:**
   ```bash
   cd /var/www/bingo_bot/admin
   npx tsx scripts/check-platform-limits.ts
   ```

2. **Если конфигурация не найдена, обновите пароли:**
   ```bash
   npx tsx scripts/update-casino-passwords.ts
   ```

3. **Для Mostbet проверьте конфигурацию:**
   - Убедитесь, что в БД есть `mostbet_api_config`
   - Проверьте, что все поля заполнены (api_key, secret, cashpoint_id)

4. **Перезапустите приложение:**
   ```bash
   cd /var/www/bingo_bot
   pm2 restart bingo-admin
   ```

5. **Проверьте логи:**
   ```bash
   pm2 logs bingo-admin --lines 100 | grep -i "platform limits\|melbet\|mostbet"
   ```

## 📝 После исправления:

После выполнения всех шагов проверьте лимиты в админ-панели:
- Откройте `/dashboard/limits`
- Убедитесь, что Melbet и Mostbet показывают реальные значения (не 0.00)

