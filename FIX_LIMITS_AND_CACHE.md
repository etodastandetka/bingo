# 🔧 Исправление проблем с лимитами и кешем Next.js

## 🚨 Проблемы:

1. **Ошибки 401/403 при получении лимитов** - пароли казино не обновлены в БД
2. **Ошибки Next.js Server Actions** - проблема с кешем после деплоя

## ✅ Решение:

### Вариант 1: Использовать готовый скрипт (рекомендуется)

```bash
cd /var/www/bingo_bot
chmod +x fix-limits-and-cache.sh
./fix-limits-and-cache.sh
```

### Вариант 2: Выполнить команды вручную

```bash
# 1. Перейти в директорию admin
cd /var/www/bingo_bot/admin

# 2. Обновить пароли в БД
npx tsx scripts/update-casino-passwords.ts

# 3. Остановить PM2 процесс
pm2 stop bingo-admin

# 4. Очистить кеш Next.js
rm -rf .next
rm -rf node_modules/.cache

# 5. Пересобрать проект
npm run build

# 6. Перезапустить PM2
cd /var/www/bingo_bot
pm2 restart bingo-admin
pm2 save

# 7. Проверить статус
pm2 status bingo-admin
pm2 logs bingo-admin --lines 50
```

## 📋 Что делает скрипт:

1. ✅ Обновляет пароли казино в БД:
   - 1xbet: `u2Z6WRZkNB`
   - Melbet: `pUdKHv4SoV`
   - WowBet: `VBjDv9GKGR`
   - Winwin: `sobaTuO8cc`
   - 1xCasino: `uuA6AHBOhp`

2. ✅ Очищает кеш Next.js (устраняет ошибки Server Actions)

3. ✅ Пересобирает проект с новыми паролями

4. ✅ Перезапускает приложение

## 🔍 Проверка после исправления:

1. **Проверьте логи:**
   ```bash
   pm2 logs bingo-admin --lines 50
   ```

2. **Проверьте лимиты в админ-панели:**
   - Откройте `/dashboard/limits`
   - Убедитесь, что лимиты загружаются (не 0.00 для всех платформ)
   - Проверьте, что нет ошибок 401/403 в логах

3. **Проверьте работу страниц:**
   - Убедитесь, что нет ошибок "Failed to find Server Action"
   - Все страницы должны загружаться нормально

## ⚠️ Важно:

- После обновления паролей приложение должно использовать новые пароли из БД
- Если лимиты все еще показывают 0.00, проверьте:
  - Что пароли правильно сохранены в БД
  - Что API платформ доступны
  - Логи на наличие ошибок 401/403

## 🐛 Если проблемы остаются:

1. **Проверьте БД напрямую:**
   ```bash
   cd /var/www/bingo_bot/admin
   npx prisma studio
   # Или через SQL:
   # Проверьте таблицу BotConfiguration, ключи типа '1xbet_api_config'
   ```

2. **Проверьте конфигурацию:**
   ```bash
   # Убедитесь, что getCasinoConfig загружает из БД
   # Проверьте файл admin/lib/casino-config.ts
   ```

3. **Проверьте логи API:**
   ```bash
   pm2 logs bingo-admin | grep -i "api error\|unauthorized\|forbidden"
   ```

