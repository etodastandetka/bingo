# 🔧 Обновление конфигурации Mostbet

## 📋 Новые данные для Mostbet:

- **API Key**: `8ff736b6-43bf-4502-afdd-c222de58b03c`
- **Secret**: `5c10831e-ebab-42fd-b27e-53c2003f52be`
- **Cashpoint ID**: `92905`

## ✅ Что было исправлено:

1. **Поддержка строкового/числового cashpoint_id** - cashpoint_id может быть строкой или числом
2. **Правильное форматирование API key** - автоматически добавляется префикс `api-key:`
3. **Улучшено логирование** - добавлены логи для диагностики запросов к API

## 🚀 Инструкция по обновлению на сервере:

```bash
cd /var/www/bingo_bot
git pull

# 1. Обновить конфигурацию Mostbet в БД
cd admin
npx tsx scripts/update-mostbet-config.ts

# 2. Перезапустить приложение
cd ..
pm2 restart bingo-admin

# 3. Проверить логи
pm2 logs bingo-admin --lines 50 | grep -i "mostbet\|platform limits"
```

## 🔍 Проверка работы:

После обновления проверьте логи на наличие:
- `[Platform Limits] Mostbet: используем конфигурацию из БД, cashpoint_id=92905`
- `[Mostbet Balance] Request: URL=..., cashpoint_id=92905`
- `[Mostbet Balance] Response OK:` - успешный ответ с балансом
- `[Mostbet Balance] Parsed balance: XXX` - распарсенная сумма баланса

Если лимит Mostbet показывает 0.00, проверьте:
1. Что конфигурация обновлена в БД
2. Что cashpoint_id сохранен правильно (92905)
3. Логи на наличие ошибок 401/403/400

## ⚠️ Важно:

- **Cashpoint ID** - сохраняется как строка, но может быть числом (92905)
- **API Key сохраняется без префикса** - код автоматически добавит `api-key:` при использовании
- **Secret никогда не логируется** - только первые символы для диагностики

