# 🚨 ИНСТРУКЦИЯ ПО ИСПРАВЛЕНИЮ АДМИНКИ НА СЕРВЕРЕ

## ✅ Что было исправлено:

1. ✅ Убран режим `output: 'standalone'` из `next.config.js` (вызывал ошибки 404 для статических файлов)
2. ✅ Улучшена обработка ошибок на всех страницах
3. ✅ Исправлены все ошибки TypeScript
4. ✅ Проект успешно собирается без ошибок

## 📋 Команды для выполнения на сервере:

```bash
# 1. Перейдите в директорию проекта
cd /var/www/bingo_bot

# 2. Обновите код из репозитория
git pull

# 3. Перейдите в директорию admin
cd admin

# 4. Установите зависимости (если нужно)
npm install

# 5. Сгенерируйте Prisma Client
npm run db:generate

# 6. Соберите проект
npm run build

# 7. Вернитесь в корень и перезапустите PM2
cd ..
pm2 restart bingo-admin

# 8. Проверьте статус
pm2 status bingo-admin
pm2 logs bingo-admin --lines 50
```

## 🔧 Альтернативно: используйте скрипт обновления

```bash
cd /var/www/bingo_bot
chmod +x update-admin.sh
./update-admin.sh
```

## ⚠️ Важно:

- После обновления проверьте логи: `pm2 logs bingo-admin`
- Если есть ошибки, проверьте что порт 3001 не занят: `netstat -tulpn | grep 3001`
- Проверьте что nginx работает: `sudo systemctl status nginx`

## 🎯 После обновления должно работать:

- ✅ Все страницы админки загружаются без ошибок 404
- ✅ Статические файлы Next.js загружаются корректно
- ✅ Все API эндпоинты работают
- ✅ Нет ошибок в консоли браузера

