# Быстрая настройка Git токена на сервере

## ❌ Проблема: Ошибка 403 "Write access to repository not granted"

Эта ошибка означает, что токен либо не имеет прав, либо введен неправильно.

## ✅ Решение

### Вариант 1: Встроить токен в URL (быстро, для сервера)

На сервере выполните:

```bash
cd /var/www/bingo_bot

# Вставьте ваш токен (тот что вы скопировали с GitHub)
git remote set-url origin https://etodastandetka:ВАШ_ТОКЕН@github.com/etodastandetka/bingo.git

# Проверьте что работает
git pull
```

**Где взять токен?**
- Если вы только что создали токен "dastik", скопируйте его с страницы GitHub
- Или создайте новый: https://github.com/settings/tokens
- **ВАЖНО**: При создании выберите scope `repo` (полный доступ к репозиториям)

### Вариант 2: Использовать credential helper (безопаснее)

```bash
cd /var/www/bingo_bot

# Настройте сохранение credentials
git config --global credential.helper store

# Сделайте pull и введите:
git pull
# Username: etodastandetka
# Password: [вставьте ваш токен, не пароль!]

# Теперь токен сохранен и больше не нужно вводить
```

### Вариант 3: Через переменную окружения (временный доступ)

```bash
cd /var/www/bingo_bot

# Установите переменную перед командой
GIT_ASKPASS=echo GIT_TERMINAL_PROMPT=0 git -c credential.helper='!f() { echo "username=etodastandetka"; echo "password=ВАШ_ТОКЕН"; }; f' pull
```

## 🔍 Проверка прав токена

Убедитесь что ваш токен имеет правильные права:

1. Перейдите: https://github.com/settings/tokens
2. Найдите ваш токен (например "dastik")
3. Проверьте что он имеет scope **`repo`** (Repositories - Full control of private repositories)
4. Если прав нет - создайте новый токен с правами `repo`

## 🧪 Тестирование

После настройки проверьте:

```bash
git fetch
git pull
git status
```

Если все работает без запроса пароля - отлично! ✅

## 🔒 Безопасность

⚠️ **ВНИМАНИЕ**: Если вы встроили токен в URL, он будет виден в:
- `git config --get remote.origin.url`
- Файле `.git/config`

Для production серверов лучше использовать:
- SSH ключи
- Credential helper с безопасным хранилищем
- Или переменные окружения

