# 🔧 Исправление ошибки "refusing to merge unrelated histories" на сервере

## ❌ Проблема:
При выполнении `git pull` на сервере возникает ошибка:
```
fatal: refusing to merge unrelated histories
```

## ✅ Решение:

### Вариант 1: Разрешить объединение несвязанных историй (рекомендуется)

Выполните на сервере:

```bash
cd /var/www/bingo_bot

# Получаем последние изменения
git fetch origin

# Делаем pull с разрешением объединения несвязанных историй
git pull origin main --allow-unrelated-histories

# Если возникнут конфликты, разрешите их и сделайте:
# git add .
# git commit -m "Merge unrelated histories"
```

### Вариант 2: Принудительная синхронизация (если нет важных локальных изменений)

⚠️ **ВНИМАНИЕ**: Этот вариант удалит все локальные изменения на сервере!

```bash
cd /var/www/bingo_bot

# Сохраняем текущее состояние (на всякий случай)
git stash

# Получаем последние изменения
git fetch origin

# Принудительно синхронизируем с удаленной веткой
git reset --hard origin/main
```

### Вариант 3: Через скрипт обновления (если используете update-admin.sh)

Измените скрипт `update-admin.sh` на сервере:

```bash
cd /var/www/bingo_bot

# Отредактируйте скрипт
nano update-admin.sh

# Найдите строку:
# git pull

# Замените на:
# git pull origin main --allow-unrelated-histories || git pull origin main
```

## 📋 Полная последовательность команд для обновления на сервере:

```bash
# 1. Перейдите в директорию проекта
cd /var/www/bingo_bot

# 2. Получите последние изменения
git fetch origin

# 3. Обновите код (с разрешением объединения несвязанных историй)
git pull origin main --allow-unrelated-histories

# 4. Если есть конфликты, разрешите их:
# git add .
# git commit -m "Resolve merge conflicts"

# 5. Обновите Admin Panel
cd admin
npm install
npm run db:generate
rm -rf .next
npm run build

# 6. Перезапустите PM2
cd ..
pm2 restart bingo-admin

# 7. Проверьте статус
pm2 status bingo-admin
pm2 logs bingo-admin --lines 50
```

## 🔍 Проверка после исправления:

```bash
# Проверьте статус git
git status

# Проверьте последние коммиты
git log --oneline -5

# Убедитесь, что синхронизировано с origin/main
git log origin/main --oneline -3
```

## ⚠️ Важно:

- Если на сервере есть важные локальные изменения, сначала сохраните их через `git stash`
- После исправления проверьте, что все работает корректно
- В будущем используйте `git pull origin main --allow-unrelated-histories` если ошибка повторится
