# 🔧 Исправление ошибки "Server Action 'x'" в Next.js

## Проблема

Ошибка:
```
Error: Failed to find Server Action "x". This request might be from an older or newer deployment. 
Original error: Cannot read properties of undefined (reading 'workers')
```

Эта ошибка возникает когда:
- Приложение было пересобрано, но процесс Next.js не был перезапущен
- Кеш Next.js устарел или поврежден
- Есть несоответствие между собранным кодом и запущенным процессом

## 🔧 Быстрое решение

### Вариант 1: Использовать скрипт (рекомендуется)

```bash
cd /var/www/bingo_bot
chmod +x fix-server-action-error.sh
./fix-server-action-error.sh
```

### Вариант 2: Выполнить команды вручную

```bash
# 1. Перейдите в директорию admin
cd /var/www/bingo_bot/admin

# 2. Очистите кеш Next.js
rm -rf .next
rm -rf node_modules/.cache

# 3. Пересоберите проект
npm run build

# 4. Вернитесь в корень и перезапустите PM2
cd ..
pm2 restart bingo-admin --update-env

# 5. Проверьте статус и логи
pm2 status bingo-admin
pm2 logs bingo-admin --lines 50
```

### Вариант 3: Полное обновление (если нужно обновить код из Git)

```bash
cd /var/www/bingo_bot
./update-admin.sh
```

## ✅ Проверка результата

После перезапуска проверьте:
1. Статус процесса: `pm2 status bingo-admin`
2. Логи (не должно быть ошибок): `pm2 logs bingo-admin --lines 50`
3. Откройте админку в браузере и проверьте, что страницы загружаются

## 💡 Почему это происходит?

Next.js использует внутренние идентификаторы для Server Actions. Когда приложение пересобирается:
- Старые идентификаторы могут оставаться в памяти запущенного процесса
- Новые идентификаторы создаются при новой сборке
- Происходит конфликт между старым и новым кодом

**Решение**: Очистить кеш и перезапустить процесс после каждой сборки.

## ⚠️ Важно

- Всегда перезапускайте процесс после `npm run build`
- Используйте `pm2 restart` вместо `pm2 stop` + `pm2 start` для более плавного перезапуска
- Флаг `--update-env` обновляет переменные окружения при перезапуске

