# 🌐 Настройка Cloudflare для скрытия IP сервера

Это руководство поможет настроить оба сайта (Admin Panel и Payment Site) через Cloudflare, чтобы скрыть реальный IP-адрес вашего сервера.

## 📋 Предварительные требования

1. **Домены уже настроены:**
   - Admin Panel: `fqxgmrzplndwsyvkeu.ru`
   - Payment Site: `gldwueprxkmbtqsnva.ru`

2. **Сервер работает:**
   - Admin Panel на порту `3001`
   - Payment Site на порту `3003` (или `3002`)

3. **Аккаунт Cloudflare** (бесплатный план подойдет)

---

## 🔧 Шаг 1: Добавление доменов в Cloudflare

### 1.1 Добавление первого домена (Admin Panel)

1. Войдите в [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Нажмите **"Add a Site"**
3. Введите домен: `fqxgmrzplndwsyvkeu.ru`
4. Выберите план (Free подойдет)
5. Cloudflare просканирует ваши DNS записи

### 1.2 Добавление второго домена (Payment Site)

Повторите шаги 1.1 для домена `gldwueprxkmbtqsnva.ru`

---

## 🔍 Шаг 2: Изменение DNS записей в Cloudflare

### 2.1 Для Admin Panel (`fqxgmrzplndwsyvkeu.ru`)

1. Перейдите в раздел **DNS** → **Records**
2. Найдите или создайте A-запись:
   ```
   Type: A
   Name: @ (или fqxgmrzplndwsyvkeu.ru)
   IPv4 address: [ВАШ_IP_СЕРВЕРА] (например, 92.51.38.85)
   Proxy status: 🟠 Proxied (ОБЯЗАТЕЛЬНО!)
   TTL: Auto
   ```
3. Если есть поддомен `www`, создайте CNAME:
   ```
   Type: CNAME
   Name: www
   Target: fqxgmrzplndwsyvkeu.ru
   Proxy status: 🟠 Proxied
   TTL: Auto
   ```

### 2.2 Для Payment Site (`gldwueprxkmbtqsnva.ru`)

Повторите шаги 2.1 для второго домена:
```
Type: A
Name: @
IPv4 address: [ВАШ_IP_СЕРВЕРА]
Proxy status: 🟠 Proxied (ОБЯЗАТЕЛЬНО!)
TTL: Auto
```

**⚠️ ВАЖНО:** Обязательно включите **Proxied** (оранжевое облако) - это скрывает ваш реальный IP!

---

## 🔐 Шаг 3: Настройка SSL/TLS в Cloudflare

### 3.1 Включение SSL

1. Перейдите в **SSL/TLS** → **Overview**
2. Выберите режим: **Full (strict)** или **Full**
   - **Full (strict)** - рекомендуется, если у вас есть валидный SSL сертификат на сервере
   - **Full** - если SSL сертификат самоподписанный или от Let's Encrypt

### 3.2 Настройка для обоих доменов

Повторите для каждого домена:
- `fqxgmrzplndwsyvkeu.ru`
- `gldwueprxkmbtqsnva.ru`

---

## 🛡️ Шаг 4: Настройка безопасности (опционально, но рекомендуется)

### 4.1 Включение защиты от DDoS

1. Перейдите в **Security** → **Settings**
2. Включите:
   - **Security Level**: Medium или High
   - **Challenge Passage**: 30 minutes
   - **Browser Integrity Check**: ON

### 4.2 Настройка Firewall Rules

1. Перейдите в **Security** → **WAF**
2. Создайте правила для блокировки подозрительных запросов
3. Включите **Managed Rules** (базовые правила включены по умолчанию)

---

## ⚙️ Шаг 5: Настройка Nginx на сервере для работы с Cloudflare

### 5.1 Обновление конфигурации Nginx для Admin Panel

```bash
sudo nano /etc/nginx/sites-available/bingo-admin
```

Обновите конфигурацию:

```nginx
server {
    listen 80;
    server_name fqxgmrzplndwsyvkeu.ru;

    # Разрешаем только запросы от Cloudflare
    # Получите IP-адреса Cloudflare: https://www.cloudflare.com/ips/
    
    # IPv4
    allow 173.245.48.0/20;
    allow 103.21.244.0/22;
    allow 103.22.200.0/22;
    allow 103.31.4.0/22;
    allow 141.101.64.0/18;
    allow 108.162.192.0/18;
    allow 190.93.240.0/20;
    allow 188.114.96.0/20;
    allow 197.234.240.0/22;
    allow 198.41.128.0/17;
    allow 162.158.0.0/15;
    allow 104.16.0.0/13;
    allow 104.24.0.0/14;
    allow 172.64.0.0/13;
    allow 131.0.72.0/22;
    
    # IPv6 (если используется)
    allow 2400:cb00::/32;
    allow 2606:4700::/32;
    allow 2803:f800::/32;
    allow 2405:b500::/32;
    allow 2405:8100::/32;
    allow 2a06:98c0::/29;
    allow 2c0f:f248::/32;
    
    deny all;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        
        # Cloudflare передает реальный IP в заголовках
        proxy_set_header X-Real-IP $http_cf_connecting_ip;
        proxy_set_header X-Forwarded-For $http_cf_connecting_ip;
        proxy_set_header X-Forwarded-Proto $cf_scheme;
        proxy_set_header CF-Connecting-IP $http_cf_connecting_ip;
        proxy_set_header CF-Ray $http_cf_ray;
        
        proxy_cache_bypass $http_upgrade;
    }
}
```

### 5.2 Обновление конфигурации Nginx для Payment Site

```bash
sudo nano /etc/nginx/sites-available/bingo-payment
```

```nginx
server {
    listen 80;
    server_name gldwueprxkmbtqsnva.ru;

    # Те же правила доступа от Cloudflare
    allow 173.245.48.0/20;
    allow 103.21.244.0/22;
    allow 103.22.200.0/22;
    allow 103.31.4.0/22;
    allow 141.101.64.0/18;
    allow 108.162.192.0/18;
    allow 190.93.240.0/20;
    allow 188.114.96.0/20;
    allow 197.234.240.0/22;
    allow 198.41.128.0/17;
    allow 162.158.0.0/15;
    allow 104.16.0.0/13;
    allow 104.24.0.0/14;
    allow 172.64.0.0/13;
    allow 131.0.72.0/22;
    
    deny all;

    location / {
        proxy_pass http://localhost:3003;  # Или 3002, в зависимости от вашей конфигурации
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $http_cf_connecting_ip;
        proxy_set_header X-Forwarded-For $http_cf_connecting_ip;
        proxy_set_header X-Forwarded-Proto $cf_scheme;
        proxy_set_header CF-Connecting-IP $http_cf_connecting_ip;
        proxy_set_header CF-Ray $http_cf_ray;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### 5.3 Применение изменений

```bash
# Проверка конфигурации
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

---

## 🔄 Шаг 6: Обновление Nameservers у регистратора домена

### 6.1 Получение Nameservers от Cloudflare

1. В Cloudflare Dashboard перейдите в **Overview**
2. Найдите раздел **"Nameservers"**
3. Скопируйте два nameserver'а (например):
   ```
   dante.ns.cloudflare.com
   lola.ns.cloudflare.com
   ```

### 6.2 Обновление у регистратора

1. Войдите в панель управления вашего регистратора домена
2. Найдите раздел **DNS** или **Nameservers**
3. Замените текущие nameservers на те, что дал Cloudflare
4. Сохраните изменения

**⚠️ ВАЖНО:** Изменения могут занять до 24 часов, но обычно происходит за 1-2 часа.

---

## ✅ Шаг 7: Проверка работы

### 7.1 Проверка DNS

```bash
# Проверка DNS для Admin Panel
dig fqxgmrzplndwsyvkeu.ru

# Проверка DNS для Payment Site
dig gldwueprxkmbtqsnva.ru

# Должны увидеть IP-адреса Cloudflare, а не ваш сервер
```

### 7.2 Проверка SSL

1. Откройте в браузере:
   - `https://fqxgmrzplndwsyvkeu.ru`
   - `https://gldwueprxkmbtqsnva.ru`
2. Должен быть валидный SSL сертификат от Cloudflare

### 7.3 Проверка скрытия IP

```bash
# Проверка через whois
whois fqxgmrzplndwsyvkeu.ru

# Должен показывать IP Cloudflare, а не ваш сервер
```

---

## 🚀 Шаг 8: Дополнительные настройки Cloudflare (опционально)

### 8.1 Включение кэширования

1. Перейдите в **Caching** → **Configuration**
2. Включите **Caching Level**: Standard
3. Настройте **Browser Cache TTL**: 4 hours

### 8.2 Настройка Speed

1. Перейдите в **Speed** → **Optimization**
2. Включите:
   - **Auto Minify**: CSS, JavaScript, HTML
   - **Brotli**: ON
   - **HTTP/2**: ON
   - **HTTP/3 (with QUIC)**: ON

### 8.3 Настройка Page Rules (если нужно)

1. Перейдите в **Rules** → **Page Rules**
2. Создайте правила для:
   - Кэширования статических файлов
   - Отключения кэширования для API endpoints

---

## 🔒 Шаг 9: Безопасность на уровне сервера

### 9.1 Обновление файрвола

Теперь можно закрыть прямой доступ к портам 80 и 443, оставив только доступ от Cloudflare:

```bash
# Разрешить только Cloudflare IPs (пример для UFW)
sudo ufw allow from 173.245.48.0/20 to any port 80
sudo ufw allow from 103.21.244.0/22 to any port 80
# ... и так далее для всех Cloudflare IP ranges

# Или просто закройте порты 80 и 443 для всех, кроме localhost
sudo ufw deny 80/tcp
sudo ufw deny 443/tcp
```

### 9.2 Проверка логов

```bash
# Проверка логов Nginx
sudo tail -f /var/log/nginx/access.log

# Должны видеть запросы с IP Cloudflare, а не реальных пользователей
```

---

## 📊 Шаг 10: Мониторинг в Cloudflare

### 10.1 Analytics

1. Перейдите в **Analytics** → **Web Traffic**
2. Здесь вы можете видеть:
   - Количество запросов
   - Трафик
   - Угрозы (блокированные атаки)

### 10.2 Security Events

1. Перейдите в **Security** → **Events**
2. Мониторьте заблокированные атаки и подозрительную активность

---

## 🛠️ Решение проблем

### Проблема: Сайт не открывается после настройки Cloudflare

**Решение:**
1. Проверьте, что DNS записи обновлены (может занять время)
2. Проверьте, что Proxy status включен (🟠 оранжевое облако)
3. Проверьте SSL режим в Cloudflare (должен быть Full или Full strict)
4. Проверьте логи Nginx: `sudo tail -f /var/log/nginx/error.log`

### Проблема: Ошибка 502 Bad Gateway

**Решение:**
1. Проверьте, что приложения запущены: `pm2 list`
2. Проверьте, что Nginx правильно проксирует на правильные порты
3. Проверьте конфигурацию Nginx: `sudo nginx -t`
4. Проверьте, что файрвол не блокирует Cloudflare IPs

### Проблема: SSL сертификат не работает

**Решение:**
1. В Cloudflare SSL режим должен быть **Full** или **Full (strict)**
2. На сервере должен быть установлен валидный SSL сертификат (Let's Encrypt)
3. Проверьте, что Nginx слушает на порту 443 с SSL

---

## ✅ Чеклист настройки

- [ ] Оба домена добавлены в Cloudflare
- [ ] DNS записи настроены с **Proxied** статусом (🟠)
- [ ] SSL/TLS режим установлен на **Full** или **Full (strict)**
- [ ] Nameservers обновлены у регистратора
- [ ] Nginx настроен для работы с Cloudflare IPs
- [ ] Файрвол настроен (опционально)
- [ ] Сайты открываются через HTTPS
- [ ] IP сервера скрыт (проверено через whois)

---

## 📝 Важные заметки

1. **Cloudflare скрывает IP только если включен Proxy (🟠 оранжевое облако)**
2. **Изменения DNS могут занять до 24 часов**
3. **Всегда проверяйте, что SSL режим правильный**
4. **Регулярно обновляйте список IP-адресов Cloudflare** (они могут меняться)

---

## 🔗 Полезные ссылки

- [Cloudflare IP Ranges](https://www.cloudflare.com/ips/)
- [Cloudflare SSL/TLS Modes](https://developers.cloudflare.com/ssl/origin-configuration/ssl-modes/)
- [Cloudflare DNS Documentation](https://developers.cloudflare.com/dns/)

---

**Готово! Теперь оба ваших сайта работают через Cloudflare и ваш реальный IP-адрес сервера скрыт! 🎉**

